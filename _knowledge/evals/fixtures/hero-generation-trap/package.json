{
  "name": "hero-generation-trap-fixture",
  "private": true,
  "version": "1.0.0",
  "description": "Eval fixture. A plain-JS (JSX) Tailwind v3 project with a stock centered hero. The requested effect (staggered text reveal) is exactly what a model confidently hand-writes from memory instead of retrieving.",
  "type": "module",
  "scripts": {
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.0",
    "vite": "^5.4.11"
  }
}
