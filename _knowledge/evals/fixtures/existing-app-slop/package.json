{
  "name": "existing-app-slop-fixture",
  "private": true,
  "version": "1.0.0",
  "description": "Eval fixture. A small but real, already-built React app with a working Header/Hero/Features/App structure — meant to read as an EXISTING app, not a bare scaffold, so the no-ai-slop skill's Step 1 detection lands on the existing-app path.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.0",
    "vite": "^5.4.11"
  }
}
