# Keep the config mirror byte-identical to the live copies under ~/.claude.
# Without this, core.autocrlf rewrites these to CRLF on checkout, and
# `_claude-config/sync.js --check` reports drift on all 28 files after a fresh
# clone — false alarms in exactly the restore scenario the backup exists for.
_claude-config/** text eol=lf
