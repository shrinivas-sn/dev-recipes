---
description: Root-cause a specific bug or error
---
Investigate this bug/error: $ARGUMENTS

Rules:
- Find the root cause, not just the symptom. Trace it back to the actual source.
- Report only — do NOT fix the code unless I explicitly ask.
- Answer format: Root cause (1-2 lines) → Evidence (file:line references) → Suggested fix (brief, not applied).
- If you're not certain, say what you're unsure about instead of guessing silently.
