---
description: Quick how-to guide for doing something in this project
---
Give me a step-by-step guide for: $ARGUMENTS

Rules:
- Base it on this project's actual code/conventions (check relevant files first, don't assume).
- Numbered steps, in order.
- Add a code snippet/command only where words alone wouldn't be clear enough — skip it otherwise.
- Match depth to the question: simple ask → quick reference steps only; complex/unfamiliar ask → add brief why/context per step so it's actually understood.
