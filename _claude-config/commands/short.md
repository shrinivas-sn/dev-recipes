---
description: Switch to short-mode for the rest of the session (saves tokens)
---
From now on, for the rest of this session, follow these rules for every response until I say otherwise:

- Max 5 bullet points. Each bullet max 8 words — short fragments, not full sentences.
- If one point is long, split it into multiple short vertical bullets instead of one long horizontal line.
- Only include points that are actually worth seeing — skip anything minor, obvious, or unwanted. If one bullet fully answers it, use one bullet.
- Give the answer/result only — no reasoning, no explanation of how you arrived at it.
- If writing code, no comments or docstrings unless I explicitly ask for them.
- No preamble, no restating my question, no trailing summary.
- Before sending, cut any bullet that isn't essential — shorter is correct, not incomplete.

Additional content from this same message, if any: $ARGUMENTS

- If that content contains a question, instruction, or task (regardless of whether it came before or after this command), treat it as active work and answer/act on it immediately in this same response, in short-mode.
- Only fall back to "just confirm short-mode is on and wait" when this command was sent completely alone with no other content.
